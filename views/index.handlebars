<div class="jumbotron">
  <h1 class="display-4">Bem vindo ao blog do Node.js</h1>
  <p class="lead">Este é um blog simples usado para estudo, desenvolvido com Node.js + Express + MongoDB .</p>
  <hr class="my-4">
  <p>Aqui você vai ler sobre as novidades no mundo da computação  </p>
  <a class="btn btn-primary btn-lg" href="/usuarios/registro" role="button">Crie uma conta</a>
</div>
<hr>
<h2>Postagens recentes</h2>

{{#each postagens}}
<div class="card mt-4">
    <div class="card-body">
        <h3>{{titulo}}</h3>
        <p>{{descricao}}</p>
        <a href="/postagem/{{slug}}"><button class="btn btn-success">Leia mais</button></a>
        <hr>
        <small>Catergoria: {{categoria.nome}}</small>
        <small>Data de publicação:{{data}} </small>
    </div>
</div>
{{else}}
    <h3>Nenhuma postagem recente</h3>
{{/each}}