<h4>Categorias: </h4>

{{#each categorias}}
<div class="card">
    <div class="card-bory">
        <a href="/categorias/{{slug}}"><h5>{{nome}}</h5></a>
    </div>
</div>

{{else}}
<h4>Ainda não há categorias para mostrar</h4>
{{/each}}